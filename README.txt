The project is a M(ongo) E(xpress) N(ode) Rest APi aropund contractor
information in a cachment area



Folder breakdown:

Model:

Houses the various mongo schemas used in serialising data - need to check

Routes:

Contains the generated REST api routes and there generic functions

db:

Is the mongoDb data used in testing the application

issues install node-sas post intall 

sudo rm -rf package-lock.json node_modules
sudo npm cache clean --force
sudo npm i --unsafe-perm node-sass

For express typings in vs code
npm install --save @types/express 